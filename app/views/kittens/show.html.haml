%h1= @kitten.name 
%hr
%h2 Kitten Stats:
%p Age: #{@kitten.age}
%p Cuteness: #{@kitten.cuteness}
%p Softness: #{@kitten.softness}
=link_to "Edit", edit_kitten_path(@kitten)
=link_to "Delete", kitten_path(@kitten), method: :delete, data: {confirm: "Are you sure?"}
%br
=link_to "<< Back", kittens_path